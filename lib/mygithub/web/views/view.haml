= haml :header_menu, :locals => {:flistpath => '..'}

.content
  = haml :search_form

  .search-summary
    <span class="keyword">#{topic_path(@path, params)}</span>#{additional_info(@path, params)}（#{@elapsed}秒）#{goto_github_project(@path)}
  ~ @record_content




